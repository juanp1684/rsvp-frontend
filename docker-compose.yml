services:
  fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rsvp-fe
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    volumes:
      - .:/app
      - fe_node_modules:/app/node_modules
    env_file:
      - .env

volumes:
  fe_node_modules:
